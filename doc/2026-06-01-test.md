# 2026-06-01 系统功能测试报告

## 测试环境

- 固件: de2shell_rtos, 152KB, SDRAM 执行
- 板卡: DE2-115, boot mode 0, COM10 @ 115200

## 问题清单

### 1. crypto bench — HEX 显示冻结

运行 `bench` 期间 HEX/LED 不再更新（uptime 卡住）。原因是 active_prog 切换为 Crypto 后，`t_status` 走 `board_status_set_program()` 分支，只显示 PROG_ID/STATE，不更新 uptime。

**修复**: `t_status` 非 shell 分支改为 `board_status_set_program(prog, state, 0, uptime)`，HEX3-0 持续刷新 uptime。

### 已完成 2. snake — VGA 边框绘制异常

VGA 边框正常显示，问题已解决。

### 4. conway (软件版)

- 需要方向键移动选定目标的支持
- HEX 在 10 0000 和 20 0A13 之间反复跳动（状态切换不稳定）
- 右侧有大块空闲区域，考虑：扩展地图 或 添加规则演示图像

### 6. expdemo — 需要特化监测面板

每个实验需要独立监测面板，面向使用者而非仅开发者。

### 已完成 7. expdemo 退出后 shell 未接管板载显示

**修复**: `exit_active_program()` 和 `stop_active_program()` 均增加 EXPDEMO channel=0 写入 + `board_status_release()` 调用，确保 VHDL 硬件 mux 切回 GPIO 模式。

### 8. 命令提示符间距

monitor (riscvasm) 提示符 `> ` 前可能有全角空格导致视觉间距异常。源码确认使用半角空格，待板测确认 VGA 字符 ROM 渲染是否正常。

### 9. conwayhw — 移动时花屏

需要差分刷新（同 info.c 的方案）。方向键已通过 CSI decode 修复。

### 10. NTT — 使用说明不够详细

### 12. synth — help 菜单过时

实际功能未测试，help 菜单很久未更新。

### 已完成 13. selfcheck — UART 文本对齐偏移

**修复**: bootloader selfcheck 格式改为 `[  OK  ]`（绿色）和 `[FAILED]`（红色），等宽对齐。移除导致 trap 的 BuildInfo 检查（bus scan 9/9 已验证总线功能）。

### 14. pxtest — 串口输出可见

8 阶段诊断输出正常，VGA 上只看到 2 张图（预期更多？）。

### 16. vgadump / vgam

数据量太大，串口无法有效测试。

## 板级验证通过项

SDRAM, FreeRTOS 4 tasks, UART shell, VGA text 80×30, CLI builtins, hello, info, life, TWM pixel mode (30s stable), ExpDemo 13 experiments, selfcheck (boot + shell), stats/heapstat/cpustat 基本功能, pxtest
